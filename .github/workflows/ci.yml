name: CI

on:
  push:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Spin up services
        uses: hoverkraft-tech/compose-action@3846bcd61da338e9eaaf83e7ed0234a12b099b72
        with:
          compose-file: ./docker-compose.yml
          up-flags: "--build --wait"

      - name: Wait for Eureka + Config (sanity)
        run: |
          timeout 60 bash -c \
            'until curl -s http://localhost:8761/actuator/health | grep UP; do echo "Waiting for eureka…"; sleep 5; done'

      - name: Smoke test
        run: |
          curl -f http://localhost:8081/actuator/health
          curl -f http://localhost:8082/actuator/health
          curl -f http://localhost:8083/actuator/health

      - name: Teardown
        if: always()
        run: docker compose down --volumes
